<nav class="navbar navbar-light bg-white shadow-sm">
    <div class="container">
        <a class="navbar-brand font-weight-bold d-flex align-items-center" href="/">
            <span style="font-size: 1.5rem; margin-right: 8px;">🌱</span>
            <div class="d-flex flex-column" style="line-height: 1;">
                <span class="text-dark font-weight-bold">BiCap</span>
                <small class="text-success font-weight-bold" style="font-size: 0.65rem; letter-spacing: 1px;">SYSTEM</small>
            </div>
        </a>
        
        <div class="d-none d-md-flex gap-3 align-items-center">
            <a href="/" class="text-muted text-decoration-none nav-link-custom">🏠 Trang chủ</a>
            <a href="/market" class="text-muted text-decoration-none nav-link-custom">🏪 Sàn giao dịch</a>
            <a href="/trace" class="text-muted text-decoration-none nav-link-custom">📱 Truy xuất</a>
            <a href="#about" class="text-muted text-decoration-none nav-link-custom">ℹ️ Về chúng tôi</a>
            <a href="#contact" class="text-muted text-decoration-none nav-link-custom">📞 Liên hệ</a>
        </div>

        <div class="d-flex align-items-center gap-2">
            <% if (typeof user !== 'undefined' && user) { %>
                <div class="dropdown">
                    <a href="#" class="d-flex align-items-center text-dark text-decoration-none" onclick="toggleDropdown(event)">
                        <div class="bg-success text-white d-flex align-items-center justify-content-center shadow-sm" style="width: 38px; height: 38px; border-radius: 50%; font-weight: bold;">
                            <%= user.username.charAt(0).toUpperCase() %>
                        </div>
                        <span class="font-weight-bold ms-2 d-none d-md-inline"><%= user.username %></span>
                    </a>
                    <div class="dropdown-menu" id="userDropdown" style="display: none; position: absolute; right: 0; top: 100%; min-width: 200px; background: white; border-radius: 0.5rem; box-shadow: 0 4px 15px rgba(0,0,0,0.1); margin-top: 8px; z-index: 1000;">
                        <a class="dropdown-item d-flex align-items-center py-2 px-3 text-decoration-none text-dark" href="/profile">
                            👤 Hồ sơ cá nhân
                        </a>
                        <% if (user.role === 'ADMIN' || user.role === 'ROLE_ADMIN') { %>
                            <a class="dropdown-item d-flex align-items-center py-2 px-3 text-decoration-none text-dark" href="/admin/dashboard">
                                ⚙️ Quản trị
                            </a>
                        <% } %>
                        <hr class="my-1">
                        <a class="dropdown-item d-flex align-items-center py-2 px-3 text-decoration-none text-danger" href="/auth/logout">
                            🚪 Đăng xuất
                        </a>
                    </div>
                </div>
            <% } else { %>
                <a href="/auth/login" class="btn btn-sm btn-outline-success">🔐 Đăng nhập</a>
                <a href="/auth/register" class="btn btn-sm btn-bicap">📝 Đăng ký</a>
            <% } %>
        </div>
    </div>
</nav>

<style>
    .nav-link-custom:hover {
        color: #4CAF50 !important;
    }
    .dropdown-item:hover {
        background-color: #f8f9fa;
    }
</style>

<script>
function toggleDropdown(e) {
    e.preventDefault();
    var dropdown = document.getElementById('userDropdown');
    dropdown.style.display = dropdown.style.display === 'none' ? 'block' : 'none';
}
document.addEventListener('click', function(e) {
    var dropdown = document.getElementById('userDropdown');
    if (dropdown && !e.target.closest('.dropdown')) {
        dropdown.style.display = 'none';
    }
});
</script>
