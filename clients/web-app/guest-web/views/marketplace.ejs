<!DOCTYPE html>
<html lang="vi">
<head>
     <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <meta name="description" content="BiCap - Nền tảng nông nghiệp sạch ứng dụng Blockchain minh bạch">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="bg-gray-100">

    <nav class="navbar navbar-expand-lg fixed-top shadow-sm transition-all">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <span class="material-symbols-rounded text-success me-2 fs-1">eco</span>
                <div class="d-flex flex-column lh-1">
                    <span class="fw-bold text-dark fs-5">BiCap</span>
                    <small class="text-success fw-bold" style="font-size: 0.7rem; letter-spacing: 1px;">SYSTEM</small>
                </div>
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Trang chủ</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/market">Sàn giao dịch</a></li>
                    <li class="nav-item"><a class="nav-link" href="/trace">Truy xuất</a></li>
                    <li class="nav-item"><a class="nav-link" href="#about">Về chúng tôi</a></li>
                    <li class="nav-item"><a class="nav-link" href="#contact">Liên hệ</a></li>
                </ul>
                <div class="d-flex align-items-center gap-3 mt-3 mt-lg-0">
                    <% if (user) { %>
                        <div class="dropdown">
                            <a href="#" class="d-flex align-items-center text-dark text-decoration-none dropdown-toggle" data-bs-toggle="dropdown">
                                <div class="bg-success text-white rounded-circle d-flex align-items-center justify-content-center me-2 shadow-sm" style="width: 38px; height: 38px; font-weight: bold;">
                                    <%= user.username.charAt(0).toUpperCase() %>
                                </div>
                                <span class="fw-semibold"><%= user.username %></span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end border-0 shadow mt-2 rounded-3 overflow-hidden">
                                <li><a class="dropdown-item py-2" href="/profile"><i class="material-symbols-rounded align-middle me-2 text-primary">person</i> Hồ sơ cá nhân</a></li>
                                <% if (user.role === 'ADMIN' || user.role === 'ROLE_ADMIN') { %>
                                    <li><a class="dropdown-item py-2" href="/admin/dashboard"><i class="material-symbols-rounded align-middle me-2 text-warning">admin_panel_settings</i> Quản trị</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider my-0"></li>
                                <li><a class="dropdown-item py-2 text-danger" href="/logout"><i class="material-symbols-rounded align-middle me-2">logout</i> Đăng xuất</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a href="/login" class="text-dark fw-bold text-decoration-none me-2">Đăng nhập</a>
                        <a href="/register" class="btn btn-bicap shadow-sm">Đăng ký ngay</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>


    <div class="container py-4" style="margin-top: 70px;">
        <!-- Hero Section -->
        <div class="card border-0 shadow-sm mb-4" style="background: linear-gradient(135deg, #E8F5E9, #C8E6C9);">
            <div class="card-body p-4">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h2 class="font-weight-bold mb-2">🏪 Sàn Giao Dịch Nông Sản</h2>
                        <p class="text-muted mb-0">Kết nối trực tiếp nông trại với người tiêu dùng - 100% truy xuất nguồn gốc</p>
                    </div>
                    <div class="col-md-4 text-end d-none d-md-block">
                        <span style="font-size: 4rem;">🥬🍅🥕</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Search & Filter -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-body">
                <form action="/market" method="GET">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="d-flex">
                                <span class="d-flex align-items-center px-3 bg-gray-100 rounded-start" style="border-radius: 0.5rem 0 0 0.5rem;">🔍</span>
                                <input type="text" name="search" class="form-control border-0 bg-gray-100" 
                                       placeholder="Tìm kiếm sản phẩm, nông trại..." 
                                       value="<%= filters.search || '' %>"
                                       style="border-radius: 0 0.5rem 0.5rem 0;">
                            </div>
                        </div>
                        <div class="col-md-3">
                            <select name="sort" class="form-control bg-gray-100 border-0">
                                <option value="">Sắp xếp theo</option>
                                <option value="price-asc" <%= filters.sort === 'price-asc' ? 'selected' : '' %>>Giá: Thấp → Cao</option>
                                <option value="price-desc" <%= filters.sort === 'price-desc' ? 'selected' : '' %>>Giá: Cao → Thấp</option>
                                <option value="rating" <%= filters.sort === 'rating' ? 'selected' : '' %>>Đánh giá cao nhất</option>
                                <option value="sold" <%= filters.sort === 'sold' ? 'selected' : '' %>>Bán chạy nhất</option>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-bicap w-100">🔍 Tìm kiếm</button>
                        </div>
                    </div>

                    <!-- Category Chips -->
                    <div class="d-flex flex-wrap gap-2 mt-3">
                        <a href="/market" class="btn btn-sm <%= !filters.category ? 'btn-success' : 'btn-outline-success' %> rounded-pill">
                            📋 Tất cả
                        </a>
                        <% categories.forEach(function(cat) { %>
                            <% if (cat.id !== 'all') { %>
                                <a href="/market?category=<%= cat.id %>" 
                                   class="btn btn-sm <%= filters.category === cat.id ? 'btn-success' : 'btn-outline-success' %> rounded-pill">
                                    <% if (cat.id === 'vegetables') { %>🥬<% } %>
                                    <% if (cat.id === 'fruits') { %>🍎<% } %>
                                    <% if (cat.id === 'grains') { %>🌾<% } %>
                                    <% if (cat.id === 'herbs') { %>🌿<% } %>
                                    <%= cat.name %>
                                </a>
                            <% } %>
                        <% }); %>
                        <label class="btn btn-sm btn-outline-success rounded-pill d-flex align-items-center gap-1">
                            <input type="checkbox" name="organic" value="true" <%= filters.organic === 'true' ? 'checked' : '' %> onchange="this.form.submit()">
                            🌿 Chỉ Organic
                        </label>
                    </div>
                </form>
            </div>
        </div>

        <!-- Stats Bar -->
        <div class="d-flex justify-content-between align-items-center mb-3">
            <p class="text-muted mb-0">
                Tìm thấy <strong class="text-success"><%= products.length %></strong> sản phẩm
                <% if (filters.search) { %>
                    cho "<strong><%= filters.search %></strong>"
                <% } %>
            </p>
        </div>

        <!-- Products Grid -->
        <div class="row g-4">
            <% if (products.length === 0) { %>
                <div class="col-12">
                    <div class="card border-0 shadow-sm text-center py-5">
                        <span style="font-size: 4rem;">📦</span>
                        <h5 class="mt-3 font-weight-bold">Không tìm thấy sản phẩm</h5>
                        <p class="text-muted">Hãy thử tìm kiếm với từ khóa khác hoặc bỏ bộ lọc</p>
                        <a href="/market" class="btn btn-bicap">Xem tất cả sản phẩm</a>
                    </div>
                </div>
            <% } else { %>
                <% products.forEach(function(product) { %>
                    <div class="col-lg-3 col-md-4 col-sm-6">
                        <div class="card border-0 shadow-sm h-100 product-card">
                            <!-- Product Image -->
                            <div class="position-relative">
                                <div style="height: 180px; background: linear-gradient(135deg, #f8f9fa, #e9ecef); display: flex; align-items: center; justify-content: center; border-radius: 0.5rem 0.5rem 0 0;">
                                    <span style="font-size: 4rem;">
                                        <% if (product.name.includes('cải') || product.name.includes('Xà lách')) { %>🥬
                                        <% } else if (product.name.includes('cà chua') || product.name.includes('Cà chua')) { %>🍅
                                        <% } else if (product.name.includes('Dưa')) { %>🥒
                                        <% } else if (product.name.includes('Ớt')) { %>🌶️
                                        <% } else if (product.name.includes('Cà rốt')) { %>🥕
                                        <% } else if (product.name.includes('Bông cải')) { %>🥦
                                        <% } else { %>🥬<% } %>
                                    </span>
                                </div>
                                <% if (product.organic) { %>
                                    <span class="position-absolute badge bg-success shadow-sm" style="top: 10px; right: 10px;">
                                        🌿 Organic
                                    </span>
                                <% } %>
                            </div>
                            
                            <!-- Product Info -->
                            <div class="card-body">
                                <h6 class="font-weight-bold mb-1"><%= product.name %></h6>
                                <p class="text-muted small mb-2">🏠 <%= product.farmName %></p>
                                <p class="text-muted small mb-2">📍 <%= product.location %></p>
                                
                                <!-- Rating & Sold -->
                                <div class="d-flex align-items-center gap-2 mb-2">
                                    <span class="badge bg-warning-subtle text-warning">⭐ <%= product.rating %></span>
                                    <span class="text-muted small">Đã bán: <%= product.sold %></span>
                                </div>
                                
                                <!-- Price & Action -->
                                <div class="d-flex justify-content-between align-items-center mt-3">
                                    <div>
                                        <span class="text-success font-weight-bold fs-5"><%= product.price.toLocaleString('vi-VN') %>đ</span>
                                        <span class="text-muted small">/<%= product.unit %></span>
                                    </div>
                                    <a href="/trace/search?batchId=BATCH-00<%= product.id %>" class="btn btn-sm btn-outline-success rounded-pill">
                                        📱 Truy xuất
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>

        <!-- Load More -->
        <% if (products.length > 0) { %>
            <div class="text-center mt-4">
                <button class="btn btn-outline-success px-4">Xem thêm sản phẩm ↓</button>
            </div>
        <% } %>
    </div>

   <footer class="bg-dark text-white pt-5 pb-3" id="contact">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4">
                    <div class="d-flex align-items-center mb-3">
                        <span class="material-symbols-rounded text-success fs-2 me-2">eco</span>
                        <h4 class="fw-bold mb-0">BiCap Farm</h4>
                    </div>
                    <p class="text-white-50 small">Nền tảng nông nghiệp sạch ứng dụng Blockchain để truy xuất nguồn gốc minh bạch, kết nối nông dân với người tiêu dùng.</p>
                </div>
                <div class="col-lg-2 col-6">
                    <h6 class="fw-bold text-uppercase mb-3">Liên kết</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="/" class="text-white-50 text-decoration-none hover-white">Trang chủ</a></li>
                        <li class="mb-2"><a href="/market" class="text-white-50 text-decoration-none hover-white">Sàn giao dịch</a></li>
                        <li class="mb-2"><a href="/trace" class="text-white-50 text-decoration-none hover-white">Truy xuất</a></li>
                    </ul>
                </div>
                <div class="col-lg-2 col-6">
                    <h6 class="fw-bold text-uppercase mb-3">Đối tác</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="/register" class="text-white-50 text-decoration-none hover-white">Đăng ký bán hàng</a></li>
                        <li class="mb-2"><a href="/register" class="text-white-50 text-decoration-none hover-white">Đăng ký vận chuyển</a></li>
                    </ul>
                </div>
                <div class="col-lg-4">
                    <h6 class="fw-bold text-uppercase mb-3">Liên hệ</h6>
                    <ul class="list-unstyled text-white-50 small">
                        <li class="mb-2 d-flex align-items-center"><i class="material-symbols-rounded me-2 fs-6">location_on</i> 123 Nguyễn Văn Linh, Q.7, TP.HCM</li>
                        <li class="mb-2 d-flex align-items-center"><i class="material-symbols-rounded me-2 fs-6">call</i> 1900 1234 56</li>
                        <li class="mb-2 d-flex align-items-center"><i class="material-symbols-rounded me-2 fs-6">mail</i> support@bicap.vn</li>
                    </ul>
                </div>
            </div>
            <hr class="border-secondary my-4">
            <div class="text-center text-white-50 small">
                &copy; 2026 BiCap System. All rights reserved.
            </div>
        </div>
    </footer>

    <style>
        .product-card { transition: transform 0.2s, box-shadow 0.2s; }
        .product-card:hover { transform: translateY(-5px); box-shadow: 0 8px 25px rgba(0,0,0,0.15) !important; }
        .position-absolute { position: absolute; }
        .position-relative { position: relative; }
    </style>
</body>
</html>
